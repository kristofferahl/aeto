
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: certificateconnectors.acm.aws.aeto.net
spec:
  group: acm.aws.aeto.net
  names:
    kind: CertificateConnector
    listKind: CertificateConnectorList
    plural: certificateconnectors
    singular: certificateconnector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.lastUpdated
      name: LastUpdated
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CertificateConnector is the Schema for the certificateconnectors
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CertificateConnectorSpec defines the desired state of CertificateConnector
            properties:
              certificates:
                description: Certificates describes the certificates to connect.
                properties:
                  selector:
                    description: Selector specifies the selectors that need to match
                      on a resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations specifies the required annotations
                          on a resource.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels specifies the required labels on a resource.
                        type: object
                      namespaces:
                        description: Namespaces specifies the namespaces to include
                          resources from.
                        items:
                          type: string
                        type: array
                    required:
                    - annotations
                    - labels
                    - namespaces
                    type: object
                required:
                - selector
                type: object
              ingress:
                description: Ingress describes the ingress resources to update.
                properties:
                  connector:
                    description: Connector defines the type of connector to use.
                    type: string
                  selector:
                    description: Selector specifies the selectors that need to match
                      on a resource.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: Annotations specifies the required annotations
                          on a resource.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: Labels specifies the required labels on a resource.
                        type: object
                      namespaces:
                        description: Namespaces specifies the namespaces to include
                          resources from.
                        items:
                          type: string
                        type: array
                    required:
                    - annotations
                    - labels
                    - namespaces
                    type: object
                required:
                - connector
                - selector
                type: object
            type: object
          status:
            description: CertificateConnectorStatus defines the observed state of
              CertificateConnector
            properties:
              lastUpdated:
                description: LastUpdated contains a timestamp for when the connector
                  last ran to completion
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
