
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: hostedzones.route53.aws.aeto.net
spec:
  group: route53.aws.aeto.net
  names:
    kind: HostedZone
    listKind: HostedZoneList
    plural: hostedzones
    singular: hostedzone
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: HostedZone
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HostedZone is the Schema for the hostedzones API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: HostedZoneSpec defines the desired state of HostedZone
            properties:
              connectWith:
                description: ConnectWith tells the operator to connect the HostedZone
                  with a parent hosted zone by upserting it's NS recordset.
                properties:
                  name:
                    description: Name of the AWS Route53 HostedZone to connect with.
                    type: string
                  ttl:
                    default: 172800
                    description: TTL used for the NS recordset created in the specified
                      AWS Route53 HostedZone.
                    format: int64
                    type: integer
                required:
                - name
                type: object
              name:
                description: Name is the desired name for the AWS Route53 HostedZone.
                type: string
            required:
            - name
            type: object
          status:
            description: HostedZoneStatus defines the observed state of HostedZone
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
