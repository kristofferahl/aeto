
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: tenants.core.aeto.net
spec:
  group: core.aeto.net
  names:
    kind: Tenant
    listKind: TenantList
    plural: tenants
    singular: tenant
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: The name of the tenant
      jsonPath: .spec.name
      name: Tenant
      type: string
    - description: Blueprint namespace/name and version
      jsonPath: .status.blueprint
      name: Blueprint
      priority: 1
      type: string
    - description: The phase describing the tenant
      jsonPath: .status.phase
      name: Phase
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Tenant is the Schema for the tenants API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: TenantSpec defines the desired state of Tenant
            properties:
              blueprint:
                description: Blueprint contains the name of the Blueprint to use for
                  the tenant
                type: string
              name:
                description: Name is the display name of the tenant
                type: string
            required:
            - name
            type: object
          status:
            description: TenantStatus defines the observed state of Tenant
            properties:
              blueprint:
                description: Blueprint is the blueprint namespace/name and version.
                type: string
              phase:
                description: Phase is the current lifecycle phase of the resource
                  set.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
